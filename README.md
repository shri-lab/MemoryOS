# MemoryOS

Project description.

## Setup Steps

1. Create a Supabase project and enable pgvector.
2. Navigate to `backend/`, create a virtual environment, install requirements, and set up `.env`.
3. Navigate to `frontend/`, install dependencies, and set up `.env`.
